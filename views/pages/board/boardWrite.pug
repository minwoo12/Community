extends ../../layout/main

block content
    form(action=`/board${routes.boardWrite}`, method="post")
        input(type="text", name="title", placeholder="Title")
        input(type="textarea", name="content")
        input(type="submit", value="Write")